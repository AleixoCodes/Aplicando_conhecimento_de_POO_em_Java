# ☕ Exercícios de Introdução a Java e Orientação a Objetos

Este repositório contém a resolução de atividades práticas voltadas para o aprendizado da sintaxe da linguagem **Java** e dos fundamentos da **Programação Orientada a Objetos (POO)**.

---

## 🧠 Conceitos Técnicos de POO Aplicados

Abaixo estão os principais pilares da Programação Orientada a Objetos exercitados nestes códigos:

### 1. Classes e Objetos
A **Classe** foi utilizada como o "molde" ou a estrutura que define o formato dos dados (ex: `Cachorro.java`), enquanto o **Objeto** é a instância concreta criada na memória durante a execução (ex: `new Cachorro()`).

### 2. Encapsulamento
No exercício do `Cachorro`, foi aplicado o conceito de encapsulamento para proteger os dados:
- **Modificadores de Acesso:** Uso de `private` nos atributos para impedir acesso direto indevido.
- **Getters e Setters:** Criação de métodos públicos (`getNome`, `setNome`) para controlar a leitura e escrita dos atributos de forma segura.

### 3. Métodos e Comportamentos
Na classe `Calculadora`, o foco foi a criação de comportamentos. Os métodos definem as ações que o objeto pode realizar. Foi praticada a assinatura de métodos:
- Definição de **tipos de retorno** (`double`, `void`).
- Passagem de **parâmetros/argumentos** para processamento interno.

### 4. Construtores
Na classe `Aluno`, foi implementado um **Construtor Personalizado**. Isso garante que, ao instanciar um objeto `Aluno`, ele já nasça com seus dados obrigatórios (nome, matrícula, notas) preenchidos, evitando objetos em estado inconsistente.

---

## 🚀 Como executar

Certifique-se de ter o [Java JDK](https://www.oracle.com/java/technologies/downloads/) instalado em sua máquina.
